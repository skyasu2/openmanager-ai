interface:
  display_name: "OpenManager QA Ops"
  short_description: "Run, record, and include conversational AI QA when needed"
  default_prompt: "Use $openmanager-qa-ops to run OpenManager QA on Vercel or local dev, include conversational AI QA for AI-related changes, and record the result."
