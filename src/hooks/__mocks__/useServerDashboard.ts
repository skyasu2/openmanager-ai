import { fn } from 'storybook/test';

export const normalizeDashboardStatus = fn();
export const matchesStatusFilter = fn();
export const useServerDashboard = fn();
export const useEnhancedServerDashboard = fn();
