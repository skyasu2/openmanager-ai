# QA-20260519 Vercel Playwright MCP Release QA

Target: Vercel production `https://openmanager-ai.vercel.app`

Deployment:
- Vercel deployment: `dpl_BdrMbVYEqzZb7vBE9Px2ukpswKpZ`
- Production version: `v8.11.176`
- Commit: `55af9601bead10653e713f602c328de703de813c`
- GitLab tag pipeline: `2535579215` success
- GitLab main pipeline: `2535579211` success

Scope:
- Deployment identity and `/api/version`
- `/dashboard` render with OTel snapshot
- AI Assistant natural-language follow-up filtering
- AI Assistant anomaly/trend tab terminology
- Incident report generation and API degradation metadata
- Browser console and AI network sanity
- Vercel usage CLI check

Results:
- Dashboard rendered 18-server OTel snapshot at 08:30 KST, slot 51/143.
- Vercel post-deploy smoke passed for version `8.11.176` and commit `55af9601bead10653e713f602c328de703de813c`.
- Network follow-up query returned a scoped filter result: `네트워크 사용률 70% 이상 서버 0대`, with `경고 임계값 기준`; it no longer fell back to the generic whole-system summary.
- Anomaly/trend tab displayed `신호 강도 90%` and did not display the old `신뢰도` label.
- Incident report UI generated one warning report. Direct API verification returned HTTP 200 with `_fallback=false`, no `X-Fallback-Response`, no `X-AI-Degraded`, `reporterPipeline.success=true`, and a time-ordered 6-event timeline.
- Browser console warnings/errors: 0.
- Observed AI network calls: `/api/ai/wake-up`, `/api/health?service=ai&soft=true`, `/api/ai/supervisor/stream/v2`, `/api/ai/nlq/extract-entities`, `/api/ai/jobs`, `/api/ai/intelligent-monitoring`, `/api/ai/incident-report`.
- Vercel usage check remained normal: effective `$11.4422`, billed `$0.0000`.

Residual notes:
- Incident report required one proxy retry (`X-Retry-Attempt=1`) before returning non-degraded success. This is acceptable for this release but should remain monitored if latency grows.
